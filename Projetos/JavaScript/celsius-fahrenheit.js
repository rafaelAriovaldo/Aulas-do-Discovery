
function selector(C, F) {
    C = "celsius";
    F = "farenhite";
if(C == "celsius"){
   console.log (conversion.c)
}
    function conversion(ready,c,f) {

         c = (ready - 32) * 5 / 9;

         f = ready * 9 / 5 + 32;


        return ready;
    }
}